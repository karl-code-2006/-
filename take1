2. 字母游戏
【问题描述】

小明最近正在学习概率论，所以对概率感兴趣。有一天，他和小华玩了一个关于概率的游戏。首先，小明给小华一个字母和一个单词。然后小华需要计算字母出现在单词中的概率。例如，小明给小华字母 a 和单词 apple。这种情况字母 a 出现在单词中的概率是 0.20000。

【输入形式】

输入由一个小写字母和一个单词组成。字母和单词之间以空格间隔。

【输出形式】

针对输入，打印保留小数点后五位的概率。

【样例输入1】

a apple

【样例输出1】

0.20000

【样例输入2】

a Apple

【样例输出2】

0.20000

【样例说明】

单词的长度不超过 200。单词里可能有大写字符，如A，计算时，忽略大小写，既当做a计算。

【评分标准】
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main() {
    char letter;
    char word[201];
    
    scanf(" %c %s", &letter, word);
    
    letter = tolower(letter);
    int length = strlen(word);
    int count = 0;
    
    for (int i = 0; i < length; i++) {
        if (tolower(word[i]) == letter) {
            count++;
        }
    }
    
    double probability = (double)count / length;
    printf("%.5f\n", probability);
    
    return 0;
}
